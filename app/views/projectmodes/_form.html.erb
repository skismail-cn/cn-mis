<div id="project_mode_form">
	<%= form_for @projectmode do |f|%>
		<% if @projectmode.errors.any? %>
			<h3><%= pluralize(@projectmode.errors.count, "Error")%> prohibitate this project mode to save</h3>
			<ul>
				<% @projectmode.errors.full_messages.each do |msg|%>
					<li><%= msg %></li>
				<% end %>
			</ul>
		<% end %>

		<div id="project_mode_form_elements">
			<p>
				<%= f.label(:name)%>
				<%= f.text_field(:name)%>
			</p>
			<p>
				<%= f.label(:status)%>
				<%= f.check_box(:status)%>
			</p>
			<p>
				<%= f.submit%>
			</p>
		</div>
	<% end %>
</div>