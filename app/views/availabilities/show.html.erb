<div id="Show_availibility">
	<table>
		<tr>
			<th>Id</th>
			<th>Nmae</th>
			<th>Status</th>
			<th colspan="3">Actions</th>
		</tr>
		<tr>
			<td><%= @availability.id%></td>
			<td><%= @availability.name%></td>
			<td><%= @availability.status%></td>
			<td><%= link_to "Edit",edit_availability_path(@availability)%></td>
			<td><%= link_to "Delete",availability_path(@availability), method: :delete, data: { confirm: 'Are you sure?' } %></td>
			<td><%= link_to "Back",availabilities_path %></td>
		</tr>
	</table>
</div>